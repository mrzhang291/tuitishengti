# 推题生题 Agent —— 数学智能命题多智能体平台（方案 v0.1）

> 目标：把现有 5 个 skill 组成的"脚本 + Cherry Studio 网关"流水线，重构为一个以 agent 角色分工为核心的项目；
> 同时解决三个存量问题：① Cherry Studio 依赖 ② 起草/复核同模型导致独立性弱 ③ 质量合同只存在于 SKILL.md 文本、靠读文本执行、易漂移。

## 1. 现状诊断（重构的依据）

| 资产 | 现状 | 重构后的定位 |
| --- | --- | --- |
| exam-ocr | 10 脚本，MinerU → 结构化题库 | 数据入口工具（不变） |
| smart-question-tagger | 18 脚本，打标 + 教师审核工作台 | 数据生产工具（不变） |
| smart-question-recommender-v3 | 17 脚本 + 本机 HTTP 工作台 + Cherry 网关 | 核心重构对象：生题引擎 agent 化 |
| generating-math-variants | 对话式变式生成 | 出题师角色的 prompt 原型 |
| html-math-to-docx | pandoc 导出 Word | 交付出口工具（不变） |
| 硬门脚本（难度合同/结构指纹/JSON 修复/蓝图校验/批次预算） | 本地 Python，与 LLM 无关 | **保留为确定性工具，永远是最终裁决者** |
| SKILL.md 合同文本 | 248 行契约，靠 agent 读文本执行 | 拆成工具 schema + 回归测试 + 角色说明书 |

关键结论：现有系统已经把"裸 API 调用"做到了极致（修复链、双审、非对称否决）。它的天花板不在规则，而在**结构**：
起草与复核同网关同模型（`model_role: draft_and_verifier`）、150 秒/2 并发的实时契约是 HTTP 请求-响应时代的产物、反馈记忆是静态 jsonl 没有真正闭环。

## 2. 设计原则

1. **非对称权威**：确定性硬门 > 独立审题师 > 出题师。agent 只"提议"，工具才"裁决"。
2. **硬门全部工具化，不做 agent 逻辑**：难度合同、结构指纹、JSON 修复、蓝图校验、批次预算这些已经被测试覆盖的 Python 代码，原样变成 agent 可调用的工具。agent 再聪明也不能绕过它们。
3. **起草/复核模型隔离**：两个角色走不同的模型路由（可不同 provider），把"独立求解"从上下文隔离升级为模型隔离。
4. **异步化**：放弃 150 秒实时契约，教师工作台从"点按钮等 3 分钟"改为"任务队列 + 逐题提交 + 通知"。agent 慢，但异步之后慢不再是缺点——换来的是允许更长的思考与校验。
5. **质量合同代码化**：SKILL.md 里的每一条硬性约束（题型闸门、5 次预算、`\left/\right` 归一、答案等价归一……）都对应一个现有函数或测试；项目化后它们进入工具层和 CI，不再依赖"agent 读文档后自觉遵守"。

## 3. 目标架构

```
┌───────────────────────────── 教师工作台（浏览器，现有 HTML 改造） ─────────────────────────────┐
│  原题错题集 │ 智能新题（任务队列视图） │ 审核 │ 打印/导出 Word（html-math-to-docx 不变）      │
└──────────────────────────────────────┬──────────────────────────────────────────────────────┘
                                       │ 任务提交 / 状态订阅（异步）
┌──────────────────────────────────────▼──────────────────────────────────────────────────────┐
│                        Agent 引擎层（DSH / Cordis 宿主）                                      │
│                                                                                              │
│  规划组                      生成组                      校验组（非对称否决权）               │
│  ├ 教学链规划师                ├ 出题师                     ├ 独立审题师（只看题面，一票否决）  │
│  │  知识点 → 教学链槽位        │  任务单+单一锚点 → 题面     ├ 难度裁判（1–5 级合同）           │
│  ├ 诊断师                      ├ 蓝图构建师                 ├ 新颖性审查（结构指纹 + 拒绝记忆）  │
│  │  多题证据 → 诊断摘要        │  安全数学骨架 → 题面       └ 质检员（汇总硬门结果）            │
│  └ 任务单签发员                │  （两条起草路径并行可用）                                       │
│  └ generation-task-spec-v2     └──────────────┬───────────────────────────────────────────────┘
│                                                                                              │
│  记忆层：教师反馈记忆 │ 拒绝记忆 │ 蓝图族状态 │ 生成历史（全部结构化持久化，规划/出题必须消费） │
│  模型路由：起草模型 / 复核模型分开配置（DeepSeek、通义、OpenAI、本地 Ollama 均可；无 Cherry） │
└──────────────────────────────────────┬──────────────────────────────────────────────────────┘
                                       │ 工具调用
┌──────────────────────────────────────▼──────────────────────────────────────────────────────┐
│   确定性工具层（现有 Python 脚本原样保留，约 50 个）                                           │
│   validate_generated_questions │ 结构指纹/去重 │ JSON 截断修复 │ 蓝图校验 │ 批次预算管理        │
│   tag_structured_bank │ assemble_class_paper │ acceptance_matrix │ ……                        │
└──────────────────────────────────────────────────────────────────────────────────────────────┘
```

### 角色清单与权威等级

| 角色 | 职责 | 输入可见范围 | 否决权 |
| --- | --- | --- | --- |
| 教学链规划师 | 知识点 → 原型巩固/同法变式/迁移提升槽位 | 题库教学证据 | 无（只规划） |
| 诊断师 | 多题证据 → 诊断摘要（5 类 gap） | 安全题池 + 作答证据 | 无（只诊断） |
| 任务单签发员 | 生成 task-spec-v2 合同（目标题型/难度/禁止结构/变化维度） | 规划 + 诊断 | 无（只写合同） |
| 出题师 | 任务单 + 恰好 1 个结构锚点 → 新题草稿 | 任务单、诊断摘要、单一锚点全文 | 无（只提议） |
| 蓝图构建师 | 安全数学骨架 → 题面（确定性构造 + 模型润色） | 蓝图库、任务单 | 无（只提议） |
| 独立审题师 | 只看题面独立求解，核对答案与解析 | **仅新题题面** | **一票否决** |
| 难度裁判 | 按 1–5 级合同判定 estimated_level | 题面 + 答案 | **一票否决** |
| 新颖性审查 | 结构指纹、同批/历史/失败记录去重 | 题面 + 指纹库 | **一票否决** |
| 质检员 | 汇总全部硬门结果，决定 commit / 补位 / 失败 | 全部工具输出 | 最终裁决（工具） |
| 教师（人） | 审核、反馈（解析错误/难度偏差/结构重复） | 教师端记录 | **最高否决权，且反馈必须进记忆** |

### 每个角色 = 什么

- 在 DSH 上：一个 subagent（或 workflow 的一个 phase），角色说明书就是现有 SKILL.md 对应章节的收口版本；
- 确定性工具 = Host 服务包装的现有脚本；
- 工作台 = DSH Client 插件（Slot UI），或继续保留现有本机 HTTP 服务、由引擎层替换其 `/api` 后端——**推荐后者起步**，教师侧零迁移。

## 4. 与"脱离 Cherry Studio"的关系

Agent 化后这个诉求自动解决：

1. 模型路由从 `CHERRY_STUDIO_*` 环境变量改为引擎配置（`draft.model` / `verify.model`），支持任何 OpenAI 兼容端点或本地模型；
2. 起草与复核角色天然分属两次独立 agent 调用，配置不同的 provider 即可实现真·独立校验；
3. `reasoning_effort`、`response_format` 等参数按 provider 能力表映射（不再是 Cherry 网关一家说了算）；
4. 由于异步化，超时契约从"150 秒整批死线"改为"每题预算 + 队列优先级"，慢模型不再拖垮整批。

唯一要保留的旧约束：**密钥只存在于环境变量/系统凭据，不进 HTML、题库、历史、日志或导出物**。

## 5. 分阶段实施

### Phase 0 — 项目化地基（约 1 周）
- git init + `.gitignore`（排除 `output/` 大文件、缓存、密钥）
- 五个 skill 收敛为一个仓库：`src/` 放脚本、`skills/` 放 SKILL.md 薄壳（agent 入口层）、`docs/specs/` 放合同、`tests/` 全量迁移
- `pyproject.toml` + CI：`python -m unittest discover` 全绿是后续一切的前提
- 兄弟路径耦合（exam-ocr → tagger → recommender 的相对路径调用）改为包内 import

### Phase 1 — 模型路由抽象（约 1–2 周）
- `GatewayConfig` 中性化：`draft` / `verify` 双配置，去掉 `providerId:modelId` 格式与 localhost 强制
- 记录命名中性化（`mode`、`generator`、`draft_source` 字段），历史数据读取兼容
- SKILL.md 与工作台文案的 Cherry 词汇全面中性化
- 验收：现有回归测试（改名后）全绿 + 用直连云 API 完成一次真实 3 题批次

### Phase 2 — 角色化生题引擎（约 3 周，核心）
- 把 draft / verify / difficulty / novelty 拆成独立 agent 角色，全部走模型路由
- 硬门脚本封装为工具，agent 只能调用、不能绕过
- 工作台后端改为任务队列（生成任务提交 → 状态订阅 → 逐题 commit），原 `/api` 接口语义保留
- 保留"单 agent 快速通道"：简单巩固题用出题师一个角色直接走，避免多角色开销
- 验收：双审门通过率不低于 Phase 1 基线；教师盲评质量不下降；批量 5 题在异步模式下无死线焦虑

### Phase 3 — 记忆闭环与蓝图管理（约 2 周）
- 教师反馈 → 结构化记忆 → 规划师/出题师在下一轮强制消费（负样本避让已是旧逻辑，升级为角色间共享记忆）
- 拒绝记忆与蓝图族状态 agent 化：蓝图构建师主动避开已拒绝结构族，用尽时自动切动态配方
- 验收：连续 3 轮"结构重复"反馈后，新一轮不再产出同指纹题

### Phase 4 — 扩展评估（持续）
- 多教师/多班级共享引擎的可行性（数据隔离、并发、成本分摊）
- 其他学科（物理/化学）的迁移成本评估——taxonomy 和蓝图是学科专属的，角色框架是学科无关的

## 6. 验收标准（项目级）

1. 原测试套件（Phase 0 迁移后）持续全绿，新增工具/角色测试覆盖每个硬门
2. 新题必须通过：双审门 + 题型闸门 + 难度合同 + 结构指纹去重 + 教师审核——与旧链路同等标准
3. 生成记录如实标注来源：`Skill 安全蓝图 → 独立审题`、`出题师起草 → 独立审题`、`结构换题 → …`（中性化后的对应文案）
4. 教师端看不到任何诊断证据、任务单、锚点与反馈记忆；这些仅教师端
5. 密钥与个人信息审计：任何 HTML/导出物/历史中零出现

## 7. 主要风险与对策

| 风险 | 对策 |
| --- | --- |
| agent 不按合同出题/输出格式漂移 | 硬门永远是工具裁决；JSON 修复链保留；schema 校验在工具层 |
| 多角色延迟与成本上升 | 异步队列 + 单 agent 快速通道 + 按题位预算（保留每题 5 次语义，去掉整批 150 秒） |
| 多角色互相冲突（审题师过严导致产率下降） | 非对称权威写死在编排层：审题师只有否决权没有修改权，换结构重试而非协商 |
| 过度工程（简单题也走全角色流水线） | 难度/题型路由：巩固题走快速通道，挑战题走全流程 |
| SKILL.md 合同文本与代码漂移 | 合同下沉为工具 schema + 回归测试；SKILL.md 只留"何时用、调用谁" |

## 8. 遗留决策点（需要你拍板）

1. **宿主形态**：DSH 为推荐宿主（Cordis 服务/工具/subagent 现成），但核心角色说明书保持 harness 无关，Codex/Claude Code 也能跑——是否接受这个双轨策略？
2. **教师工作台**：先保留现有本机 HTTP 前端、只换后端（推荐），还是直接重写为 DSH Client 插件？
3. **单机 vs 多人**：Phase 4 之前按单教师单机设计，数据结构预留多租户字段——是否同意？
